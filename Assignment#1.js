// Write a function sum that finds the sum of two numbers.  Side quest - Try passing in a string instead of a number and see what happens?

function sum(a , b) {
    let totalSum = a + b;
    
    
    
  return totalSum;
}
let sm = sum("hello" , "uin");
console.log (sm);